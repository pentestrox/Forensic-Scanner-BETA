# Forensic-Scanner-PRO
A fast multithreaded desktop forensic scanner for DLL / EXE / APK / text-based files with regex-powered secret hunting, decompilation support, severity tagging, context preview, and live progress tracking.  Built with Python + GTK3 for Linux (tested on Kali), but can run anywhere GTK3 + Python are available.
